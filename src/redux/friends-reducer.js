let initialState = {
    friendsData: [
        {id:1, name:'Dima'},
        {id:2, name:'Kate'},
        {id:3, name:'Petr'},
        {id:4, name:'Alena'},
    ],
};

const friendsReducer = (state = initialState, action) => {
    
    return state;
}

export default friendsReducer;